# Next.js Project

This workspace contains a minimal Next.js project using React and the App Router.

## Prerequisite

Install Node.js first. This environment currently does not have `node`, `npm`, or `brew` available in `PATH`.

## Getting started

```bash
npm install
npm run dev
```

Then open http://localhost:3000.